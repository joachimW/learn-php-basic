# learn-php-basic
Step by step learn PHP from basic.

In this example, will be showed using of class.
In this case, we define a Button class, and in this class have cetak() function.

This function will print message, and will be called from other file named show.php
For detail, let's these files.
